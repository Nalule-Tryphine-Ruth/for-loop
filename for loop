my_list =[1,2,3]
#in this case the loop is effective to the len
#the len is repeated as many objects as they are in the list 
 for v in range(len(my_list)):
#in this case the len is assigned 1
    my_list.insert(1,my_list[v])
       print(my_list)  
#the result will be 
#(1,1,1,1,2,3)
